rules_version = '2';
service firebase.storage {
    // Pattern 1: Root level userId (Albums, Pages, Avatars, Covers)
    match /{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

}
